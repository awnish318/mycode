#include <stdio.h>
void accept(int *p,int*q){
    scanf("%d",q);
    for(int i=0;i<(*q);i++{
        scanf("%d",&p[i]);
    }
    coid display(int *a,int n){
        for (int i=0;i<n;i++){
            printf("%d",a[i]);
        }
    }
}
int main() {

int a[20];
int n;
accept(a,&n);
display (a,n);   
}
